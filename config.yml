log: 'mcp-greeting.log'
debug: true

http:
  binding: "localhost:8080"
  endpoint_path: "/mcp"
  heartbeat_seconds: 30
  # auth_token: ""
  # allowed_origins:
  #   - "https://example.com"

greeting:
  default_message: "こんにちは！"
