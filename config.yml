log_level: 'info'    # debug, info, warn, error
log: 'mcp-greeting.log'

http:
  binding: "localhost:8080"
  endpoint_path: "/mcp"
  # auth_token: ""
  # allowed_origins:
  #   - "https://example.com"

greeting:
  default_message: "こんにちは！"
